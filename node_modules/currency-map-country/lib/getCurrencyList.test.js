'use strict';

var _index = require('../src/index');

test('getCurrencyList returns expected value', function () {
    var result = (0, _index.getCurrencyList)();
    expect(result[0]).toEqual({ abbr: 'AFA',
        name: 'Afghanistan Afghani (AFA)',
        symbolFormat: 'AFA {#}' });
});